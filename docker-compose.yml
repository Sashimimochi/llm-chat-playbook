version: "3"
services:
  llm:
    build: .
    container_name: "llm"
    command: ["streamlit", "run", "app.py"]
    tty: true
    environment:
      TZ: Asia/Tokyo
    volumes:
      - ".:/projects"
    working_dir: /projects
    ports:
      - 8501:8501
